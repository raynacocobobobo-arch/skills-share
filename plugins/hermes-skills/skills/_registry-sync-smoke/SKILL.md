---
name: registry-sync-smoke
version: 1.0.0
description: Temporary CI smoke fixture for registry auto-sync verification.
triggers:
  - registry sync smoke
---

# Registry Sync Smoke

Temporary verification fixture. Remove after the auto-sync workflow is proven.
